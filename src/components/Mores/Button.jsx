import React from 'react'

const Button = ({className}) => {
  return (
    <>
        <button className={`px-8 py-4 rounded-4xl font-light ${className}`}>START THE PROJECT</button>
    </>
  )
}

export default Button